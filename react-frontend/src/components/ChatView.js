import { Box, Container } from "@mui/material";

export function ChatView() {
  return (
    <Box
      sx={{
        height: "90vh",

        bgcolor: "background.paper",
        display: "flex",
      }}
    >
      <Box
        sx={{
          margin: "auto",
        }}
      >
        ChatView
      </Box>
    </Box>
  );
}
